# CC 交接说明

## 目标

在不离开 Pin 的情况下浏览花瓣，并把用户在内嵌页面中主动复制的图片粘贴到 Pin 画布。

## 最小范围

- 左侧“花瓣”按钮可以激活。
- 截图素材面板切换成花瓣浏览面板。
- 面板用 iframe 加载花瓣页面。
- 用户在 iframe 中自行登录、搜索和浏览。
- 用户右键复制图片后，在 Pin 画布按 `Command + V`。
- 图片复用现有本地上传接口，进入“新添加截图”，并放到当前画布中央。
- 保留“截图”按钮，支持切回本地素材。

## 暂不做

- 不开发花瓣官方 API 或反向接口。
- 不抓取花瓣 DOM、Cookie、作者信息或页面内部数据。
- 不开发浏览器扩展。
- 不做批量采集、搜索同步、花瓣图片悬浮按钮。
- 不改变现有素材文件夹和 Finder 移动逻辑。

## 已知风险

- 花瓣可能禁止 iframe 嵌入。
- 浏览器可能限制第三方 iframe 的登录 Cookie。
- 某些图片使用 CSS 背景图，右键菜单不一定提供“复制图像”。
- 剪贴板读取需要用户主动点击或粘贴动作，并可能出现浏览器权限提示。

## 交付要求

- 不触碰原工作区及其 `8765` 服务。
- 完成后更新 `VERSION_HISTORY.md`，使用 Pin 的下一个版本号 `P0.3.0`。
- 在提交信息中说明是 Pin Web 的花瓣内嵌浏览实验。
- 最终报告说明 iframe 登录是否可用、图片粘贴是否可用，以及未解决的浏览器差异。

