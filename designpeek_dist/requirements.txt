fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
Pillow>=10.0.0

# AI 分析 provider(三选一,依赖全装,装时按 .env 选用)
google-genai>=1.0.0
openai>=1.0.0
anthropic>=0.40.0

# OCR 文字识别(仅 macOS 可用,其他系统会自动跳过)
pyobjc-framework-Vision>=10.0; sys_platform == "darwin"
