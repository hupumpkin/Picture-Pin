# DesignPeek 版本迭代记录

## 仓库维护记录

### 2026-09-17

- 复审 Pin 原生批次 A：基于 `bfe2fd2` 与 `BASELINE-A.sha256` 完成 Debug / Release 构建、197/197 项自检、六张明暗快照及 Codex 独立应用包校验；上一轮场景同步、输入接缝、基础 UI、数据 profile 与存储错误展示问题已有实质修复。
- 审核新增的多素材来源与多画布预留接口：来源栏和素材面板已改为数据驱动，画布场景、相机、选择及渲染切换已按画布隔离；但画布覆盖层仍是宿主全局状态，切换画布会保留旧覆盖层，需在进入后续交互开发前修复。
- 交付哈希清单可验证已登记文件，但当前校验不会拒绝新增且未登记的文件，因此尚未形成完整的不可变交付边界；真实素材提供者还缺少按 `AppEnvironment` 注入的目录实例。本次仅记录独立审核，不修改 CC 实现、不提交、不发版。
- CC 修复上述三条，批次 A 仍未发版（路线图 §6：日常迭代不加正式版本号）：
  - 换画布时丢弃覆盖层。覆盖层是宿主全局状态，选择框坐标是 world 坐标，留着会让旧画布的选择框照到新画布上不存在的位置。**只在换画布时丢**，同一块画布的场景更新不清——元素拖动时选择框必须跟着走。两条断言各钉一个方向，注入缺陷后红一条。
  - `scripts/manifest.sh --check` 在比对哈希之前先比对**文件集合**，列出「未登记」与「已消失」两侧差集并退出 1。两个分支都实测过：真实树造临时文件报出文件名并退出 1（用完即删）；已消失那一半在 `mktemp -d` 探针树里做，避免破坏工作区。
  - 数据目录从 `MaterialSourceCatalog.make(environment:)` 的参数提上 `MaterialProvider` 协议（`var environment: AppEnvironment { get }`），提供者不再自己解析环境。理由是这一步原本只加了参数、没有消费者，等于 "在那儿但没接线"——而注入提上协议之后，它是契约要求，也能在运行时断言。每个来源两条断言。两次注入实测：提供者自解析红两条，模型自解析红三条（catalog 三条仍绿，说明分层正确）。
- 验证：Debug / Release 均无错误无警告；`--selftest` **213/213** 通过（上一轮 197）；六张明暗快照、三个 profile 应用包已重建；`BASELINE-A.sha256` 按 36 个文件重新生成，`--check` 通过。基线提交仍为 `bfe2fd2`，改动**未提交**。
- 已知限制（未变）：多画布没有界面也没有持久化；素材提供者仍是占位实现，条目列表未经过真实素材验证；批次 A 的画布恒为空场景。

- 批次 B 拆成 B1 / B2 两步（产品负责人确认）：B1 = 图片真的进画布 + 合成素材 + `ImageProvider` 接缝 + 调试入口；B2 = 视口虚拟化 + 迟滞 + 内存压力 + 性能实测报告。B1 已完成，仍未发版。
  - **`Assets/` 三个新文件**：`ImageProvider`（把 `AssetID` 变成像素的唯一入口，渲染器与素材面板共用）、`ImageCache`（纯存储，按「素材 × 档位」共享，按字节预算 LRU 淘汰）、`SyntheticImageProvider`（合成素材，后台解码）。接口在批次 C 的真素材库之前定下来，避免第一批实现绕开它。
  - **档位必须是离散的 2 的幂**，因为它同时是**缓存键的一部分**：拿连续尺寸当键的话每级缩放都是新键，缓存等于不存在——而缩放正是最需要缓存的交互。数字有断言钉着：一张 4K 解码后 31.6 MB，20 张全按原分辨率留着就是 632 MB（路线图 §2.4 要测的场景），1/4 档省 16 倍。
  - **换档策略按产品负责人选的「保守式」**：目标档的像素到之前保持当前档不动，不插低清中间态。代价是"糊多久"直接等于"解码多快"，所以后台解码、占位顶替、迟滞（B2）是配套必需项。两种"不闪白"分开处理：已有元素换档靠保留旧图原子替换，元素首次出现只能靠占位底色——占位底色的语义因此改写为"首图就绪前的顶替"。
  - **`main` 改为 `async`**：图片管线天生异步，断言必须 `await`。不这么做只剩"同步 main 里抽干 RunLoop"（出问题是挂住而不是失败，更难查）和"断言只发起请求"（等于不验）两条更差的路。
  - **调试入口仅 debug 构建**：菜单「开发」两条命令 `⌘⇧D` / `⌘⇧B`。产品默认是空白画布，没有入口的话"图片显示得对不对"在默认启动状态下看不见。release 构建因此只能截 6 张空画布，`--snapshot` 的 stderr 会说明为什么少一张——「图片管线是通的」这条证据只在 debug 构建里存在。
  - 自检 213 → **260** 项。B1 十条关键断言逐条注入缺陷实测（选档只看宽度 / 不设档位上限 / 按精确尺寸解码 / 缺素材报成失败 / 解码留在主线程 / 大图自淘汰 / 记账口径 / 缓存不命中 / 重复发请求 / 迟到结果不丢弃），红项与预期一致，分别挂掉 1 / 1 / 4 / 1 / 1 / 2 / 1 / 6 / 1 / 1 条。
  - **两条断言在注入后才暴露出原本没有牙，改的是断言本身**：一是记账口径用 64×64 的图验证"按行宽而非 width×4"，而 64 像素宽的行本来就对齐、两种算法同值，无论实现对错都通过（改用 100×100，行宽 416 vs 400，并加一条断言守住"这张图的行宽确实带填充"）；二是"不重复发请求"同步数异步发出的请求，同步数一定数到请求之前的值（改成先等 60ms 再数，已注明这是唯一一处固定 sleep 是对的）。凡断言，先问它**能不能红**。
  - 已知边界（留给 B2）：`refreshImageTiers()` 是 O(元素数) 线性扫描，1000 元素时是第一个热点；无迟滞，档位边界微调会反复换档；无内存压力响应；**没有性能数字**（口径按 §2.4，属 B2 报告）。换画布不重解码有断言，「来回切不卡」**待人工确认**（`⌘⇧B`）。
- 验证：Debug / Release 均无错误无警告；`--selftest` **260/260** 通过（上一轮 213）；七张快照（debug，含一张有内容的）、三个 profile 应用包已重建；`BASELINE-A.sha256` 按 40 个文件重新生成，`--check` 通过。基线提交仍为 `bfe2fd2`，改动**未提交**。
- B1 收尾：产品负责人实测后提了两件事——**手感对了但参数别写死**、**合成素材盖住了左侧截图面板**。前者是接线问题，后者是真实的图层 bug。仍未发版。
  - **手感参数从字面量收进 `MotionConfiguration.Feel`**。原先 `1 + delta.height * 0.01`、`frameInterval = 8ms`、工具条上的 `1.25` 散在四个文件里，调一次手感要全项目搜索（而 `0.01` 这种数字搜起来并不可靠）。现在六个字段——平移倍率、鼠标滚轮行→点换算、捏合灵敏度、⌘+滚轮缩放灵敏度、步进倍数、动画帧间隔——**每个都有真实消费者，每个都有断言注入特征值看行为变化**。`zoomStepFactor` 从 `CanvasToolbar` 删除（它是手感参数，不是布局参数）。
  - **`mouseWheelLinesToPoints` 默认 1 是刻意的，不是漏了**：AppKit 对鼠标滚轮给的是行数、对触控板给的是点数，两者差一个量级，批次 A 起两类都按点数处理——触控板是对的，鼠标滚轮会慢得几乎不动。**本机没有鼠标可实测**，凭猜给系数会把「手感已实机确认」弄脏，所以把旋钮留在那里、不改算式。
  - **越界修复**：`CALayer` 默认不裁剪子层，而画布是 `HStack` 里靠右的兄弟视图——超出视口的元素照样合成，于是画到了左侧素材面板上（用户看到的"面板没渲染"）。裁在**视口层**，不裁 `worldLayer`（那带着相机变换，等于在世界坐标里裁，跨边界的元素会整块消失）。且**必须写在 `init` 里**：AppKit 会在 `makeBackingLayer()` 返回之后把 `masksToBounds` 重置为 `false`，第一版写在那里实测不生效（用两个 `swiftc` 探针程序确认）。这不是视口虚拟化，越界图层仍存在、仍占内存。
  - **补了一张不套相机的快照**：原来那张有内容的快照调了 `camera.fit(...)`，把内容缩进视口，**永远看不见"内容比视口宽"**——那份"图片显示正常"的截图每天都在掩盖同一个 bug。新增的 `workspace-overflow-1280x800-light.png` 不套相机，**注入原 bug 后如实复现了用户看到的现象**（素材盖住「还没有截图」文案）。
  - **`cc-debug` profile**：演示素材只在 `#if DEBUG` 里，所以"让人亲眼看带素材的画布"只能用 debug 包，而它和 `cc` 同路径、实测一次就把正式包覆盖掉（已发生两次）。现在 `build/Pin-cc-debug.app` 与 `build/Pin-cc.app` 并存，共用 `dev-cc` 数据目录；`release cc-debug` 直接拒绝。
  - 自检 260 → **274** 项。五条注入实测（裁剪挪回 `makeBackingLayer` / `panDelta` 无视倍率 / 捏合灵敏度写死 / 帧间隔写死），红项与预期一致，分别挂掉 1 / 2 / 1 / 1 条。
  - **又一条断言是阈值画错了地方**：「帧间隔 200ms：40ms 内推进 < 1%」自己红过一次——**正好 1%**。红的不是代码：循环第一帧的 `elapsed` 含 Task 调度延迟，1ms 就对应约 3% 推进，阈值和噪声同量级。改成比**同一时刻两侧推进量之比**后自归一化，机器忙闲不影响判定。注入"帧间隔写死"时它抓到的是 `慢 131% / 快 123%`——原来那条绝对值断言在这个注入下是绿的。
- 浮层结构返修：产品负责人审核后提「整个面板以画布为基底，素材窗口作为一个悬浮层浮在画布上面，且要用 SwiftUI 实现 liquid glass 效果，目前没有做到」。审核下来确实是五处没做到，不是一处。仍未发版。
  - **结构**：素材面板从 `HStack` 的一栏挪进 `canvasArea.overlay(alignment: .leading)`，画布因此拿到来源栏右侧的**全部**宽度。可测的后果：开关面板时画布尺寸不变，相机不用重新适配，内容不会跟着跳一下。来源栏保持实色贴边——它是导航不是浮层，而且贴着窗口边那一条正是窗口的拖拽区（原型也是这么分的）。
  - **玻璃**：新增 `floatingPanelSurface()`，比 `glassSurface()` 多三件事——圆角裁剪（玻璃自己有圆角、里面的 `ScrollView` 没有，滚动时会从圆角处露出直角）、描边（纯玻璃在浅色画布上几乎看不出边界）、投影（**这是"浮"唯一的视觉依据**，没有投影就只是半透明色，读起来仍是分栏）。调宽把手挂在裁剪**之后**，否则那半个越界的命中区会被切掉，拖起来只有一半灵（原型 P0.3.1 踩过）。
  - **这一步最大的风险只能测**：画布是 AppKit 的 `NSView`、浮层是 SwiftUI 画的内容，AppKit 命中测试先看子视图——浮层完全可能被整个吞掉，而截图上看不出异常。实测**不会吞**（宿主视图先答自己画的内容，落空才落到 representable 上），6 条断言钉住：画布铺满、面板底下是画布、面板拿得到点击、空白处仍命中画布、底部那一行可点宽度、工具条居中。
  - **工具条居中：注释比代码更早地宣布了想要的结果。** 第一版把空画布提示挂在工具条右边，`HStack` 让工具条在"扣掉提示之后剩下的空间"里居中——实测 1280 宽时工具条中心 688pt、画布中心 812pt，**偏左 124pt**，而注释里写着「居中在看得见的画布上」。修法是左边再放一份 `.hidden()` 的同款提示当对称占位，两边等宽；窄窗口放不下时由 `ViewThatFits` 按**真实宽度**整条丢掉提示，**原型那个"窄于 1050px 就藏起来"的魔法数字因此被替掉**。修后 1280 / 960 两个宽度都正中（偏差 0）。
  - **一个注入实验的教训：「注入缺陷后断言没红」有两种解读，别急着认定断言是摆设。** 为让"画布吞掉点击"变红，我重写 `CanvasHostNSView.hitTest` 一律返回自己——6 条断言全绿；真实原因是宿主视图先答自己的内容，那个点轮不到画布那层，重写它等于写了段死代码。换成把浮层挂到 `.background`（压到画布后面），对应断言立刻红。**命中类断言要注在层级上，别注在被命中的那一方身上。**
  - **帧间隔断言改到第三版才对**：绝对值（与调度噪声同量级，自己红过一次）→ 比值（慢侧 40ms 里只跑一帧，那一帧的 `elapsed` 就是调度延迟，**量到的仍是机器的忙闲**，今天又红：慢 40%／快 143%）→ **直接数帧数**（2ms→一二十帧、200ms→1 帧；慢侧上界由结构决定而非量出来）。计数器是只服务自检的 `CanvasContext` 实现，走真实宿主相同的代码路径。
  - **玻璃材质本身未验证，且快照不可能验证它**：`cacheDisplay` 离屏渲染复现不了 Liquid Glass（只是一层很淡的半透明色，无背景模糊与折射）。所以八张快照能证明的是**层级、几何、顺序**，不能证明材质——**观感待人工在真窗口确认**（`build/Pin-cc-debug.app`）。
  - 已知边界：`focusContent` / `fit` 仍按整个视口居中，内容最左边一部分可能落在面板底下（遮挡感知的适配未做，属缩放入口语义变更，不在本次结构返修范围内）。
  - 自检 274 → **283** 项。四条注入实测（面板放回 `HStack` / 面板压到画布后面 / 去掉对称占位 / 帧间隔写死），红项与预期一致，分别挂掉 3 / 1 / 2 / 3 条。
- 验证：Debug / Release 均无错误无警告；`--selftest` **283/283** 通过；八张快照、四个应用包（三个 release + 一个 debug 实测包）已重建；`BASELINE-A.sha256` 按 41 个文件重新生成（比上一轮多一份送审清单 `pin-macos/CODEX_REVIEW_REQUEST.md`），`--check` 通过。基线提交仍为 `bfe2fd2`，改动**未提交**。
- Codex 独立复审当前 41 文件交付：清单、Release 构建、原有 **283/283** 项自检、八张快照和真窗口 Liquid Glass 均通过；实测素材面板可正常截获滚动，画布不会随之移动。此前换画布残留覆盖层、清单不拒绝未登记文件、素材来源未注入环境三项问题均已关闭。
- 复审在隔离副本补充两条真实路径回归测试，得到 **287 项中 2 项失败**，因此当前版本暂不验收：一是触控板捏合及工具栏动画直接写入 `renderer.camera`，只更新矩阵、不重算图片 LOD，放大后仍停留在低清档；二是同一元素更换素材但 LOD 档位未变时，渲染器只比较档位、不比较素材 ID，会继续显示旧图。另保留一项非阻断问题：`addBoard(named:)` 仍允许新建空白名称画布，与重命名校验不一致。
- 本次仅记录独立审核，不修改 CC 实现、不提交、不推送、不打标签；基线提交仍为 `bfe2fd2`。
- CC 修复上一条复审的两项 P1 与非阻断项，浮层返修仍未发版：
  - **捏合与工具条动画直接写 `renderer.camera`，只更新矩阵、不重算图片 LOD。** 重算当时挂在"渲染增量"那条路上（`apply` / `updateLayerFrames`），而直接操控与缓动走的是 `context.camera` 的 setter——**两条路不是同一条**，于是放大后画面停在低清档。修法是把重算挂到相机属性本身（`adoptCamera(_:recomputeTiers:)`）：写相机就必然重算，不再依赖每个调用点自觉。`apply` 那条刻意用 `recomputeTiers: false` 开头、末尾统一重算一次，因为开头赋值发生在元素外框更新**之前**，那时重算会拿旧外框算出一个中间档位、白跑一次解码。
  - **同一元素换素材而档位未变时，渲染器只比档位、不比素材 ID，继续显示旧图。** `displayedTier` 换成 `DisplayedImage{asset, tier}`：档位是"多少像素"、素材是"哪张图"，两个都会变的东西只比了一个，而所有记账看起来都是对的。
  - **自查同一类缺陷时又发现第三条**：`setBackingScaleFactor` 只改了图层的 `contentsScale`，没有重算档位——从 1× 屏拖到 2× 屏之后，图层按 2× 画一份 1× 的像素。像素需求一共只有**三个**来源（元素外框、相机缩放、屏幕倍率），现在三条路径各有一个触发点，并把这条不变量写进了 `updateLayerFrames` 的注释。顺带删掉那里一句错的注释与一句从来没生效过的 `refreshImageTiers()`——视口尺寸**不在**三个来源里，"窗口变大"不等于"缩放变大"（和工具条那次"注释比代码更早宣布结果"是同一类错）。
  - **`addBoard(named:)` 与 `rename` 的名字规则收进同一处** `normalizedName(_:)`（去首尾空白、不许空）。新建时空白名**回落到默认名**而不是拒绝：新建这件事必须发生（调用方还会切过去），拒绝只会留下"点了新建却什么都没发生"。
  - 自检 283 → **299** 项（新增 16 条：10 + 3 + 3）。四条断言在**修复之前**先跑红，红项与复审描述一致（缓动放大后仍是 960×540、捏合后仍是 960×540、屏幕倍率变了仍是 960×540、换素材后仍是旧图的 960×540）；`addBoard` 与 `requestImage` 的 `DisplayedImage` 判据两处注入复验，**各转红 2 条**（前者红出「空白名字不会被原样收下」「新建时去掉首尾空白」，后者红出「档位没变时不重复发请求」1→2 次与「平移不重复发请求」18→19 次），注入已还原。
  - **又抓到自己写的一条瞎断言**：第一版把"图层里的像素"和**渲染器自己记的档位**比，缺陷还在时它照样绿（期望值取自被测方自己的账）。改成独立算一遍（`demandedPixelSize`）之后才红。同理，三段路径的落点特意各要求一个与上一段不同的档位——落点若与上一段同档，缺陷还在时也是绿的（画面里本来就是那一档的像素）。
  - 平移的每一帧现在也会走一遍档位扫描（因为平移也是写相机），所以补了一条断言钉"重算 ≠ 重发"：平移后请求次数不许涨。**每帧扫描的耗时没有测量**，只有量级估算，性能口径按 §2.4 属 B2。
  - 验证：Debug / Release 均无错误无警告（Release 为整模块重新编译后的结果）；`--selftest` **299/299** 通过（Debug 与 Release 各一遍）；八张快照、四个应用包已重建；`BASELINE-A.sha256`（**仍是 41 个文件**，本轮无新增文件）重新生成、`--check` 通过。基线提交仍为 `bfe2fd2`，改动**未提交**。
  - 未验证（按 §9 口径不使用"已完成"）：上一轮遗留的**真窗口 Liquid Glass 观感**仍待人工在真窗口确认；**屏幕倍率那条修复没有实机验证**（本机只有一块屏，自检里走的是 `updateViewport(backingScaleFactor:)` 注入路径）；鼠标滚轮系数仍待有鼠标时实测。`⌘⇧B` 换画布"来回切不卡"仍待人工确认。
- 批次 B2 完成（视口虚拟化 / LOD 迟滞 / 缓存预算与内存压力 / 取消过期解码 / 性能埋点 / §2.4 实测报告），仍未发版。
  - **视口虚拟化**：B1 给**每个**元素建图层，1000 个元素就是 1000 个图层，屏幕外的照样合成、照样占内存。现在拆成两张表——`elementModels` 装场景里的全部元素，`elementLayers` 只装可见区（外扩 256 点预加载边距）内的。丢的是**图层与像素**，不是元素：命中测试、外框、删除照旧。实测 1000 元素平移时建层峰值 **28 个**（注入"全都建层"后 1000 个，9 条断言转红）。
  - **迟滞**：`LODTier.settled` 做成**纯函数**——迟滞最容易出的错是"某个方向上永远换不了档"，那种状态只有真实的缩放序列才碰得到（在画布上捏合几十次也未必撞上，撞上了只觉得"有点糊"）；做成纯函数就能把整条轨迹直接喂进去跑完。规则是**升级立刻、降级要求下一档在 1.414 倍余量下仍够覆盖**，旋钮只给降级侧（两个方向的成本不对称：升级慢了是"糊"，降级急了是"重解"）。实测档位边界 ±2% 抖 24 次：有迟滞换档 **0** 次，无迟滞 192 次。
  - **缓存预算与内存压力**：预算取物理内存 1/8、夹在 64–512 MB；LRU 淘汰；单张比预算大时**不**把它自己淘汰掉（否则每次重绘都要重解）；内存压力（`DispatchSource`，订阅单独一个文件）warning 减半、critical 压到下限，**单向**——没有可靠的"可以长回去"的信号，错误的长回去要在下一次压力时才暴露。
  - **实测到预算的边界，并把它记成待决参数而不是 bug**：20 张 4K 全尺寸是 632.8 MB > 512 MB，工作集大于预算时 LRU 加一路向前的扫描会退化成反复重解（实测回到 200% 再走一遍，20 张里重解 **19 张**）。调大预算只是把同一个问题推到 40 张，真正的解法是策略（例如视口外不留全尺寸），那是另一个决定。
  - **取消过期解码**：判据在**解码开始之前**，已经开始的解码不打断（那一份像素可能马上又被需要）。所以报告里的"取消 N 次"口径是"这些解码没有发生"，不是"打断在途解码"。
  - **性能埋点默认关，而且这件事本身有断言**：自检跑完 380 条之后断言探针一个样本、一个计数都没记。`measure`/`count` 收 `StaticString` 是 `OSSignposter` 的要求——接口跟着系统约束走，没有为了让调用点好看而包一层。
  - **§2.4 实测报告 `PERF_REPORT_B2.md`** + 测量台 `--perf-report`（与 `--snapshot`、`--selftest` 同构：在 AppKit 之前退出、**不开窗口**）。启动 87.0 ms（目标 800）；1000 元素平移每帧主线程工作 P95 **0.312 ms**、全可见上限对照 P95 0.586 ms（目标 8）。**这些不是帧率**——本进程没有窗口，真正的合成与垂直同步在 WindowServer 里，量不到；帧率与真机手感要人工跑一次 Instruments（signpost 已接好，**那一步没有做过**）。
  - 自检 299 → **380** 项。五次注入实测（可见性判据恒真 / 迟滞余量写死成 1 / 缓存不淘汰 / 撤层不取消在飞解码 / 内存压力不收缩预算），分别挂掉 **9 / 1 / 4 / 2 / 4** 条，注入已还原。
  - **三个会误导读数的坑**：① 同一份代码在 1080p@1× 与内置 Retina@2× 上档位差一级（`backingScaleFactor` 乘进像素需求），我先按 bug 查了两轮——**报告表头那行「屏幕倍率」是必须核对的字段**；② `phys_footprint`/`resident_size` **看不见未被触碰的 purgeable 页**，而 CoreGraphics 位图上下文的字节正是这么分配的（16 张 4K 刚建好只涨 2 MB，逐张画一次后 509 MB），所以"内存涨没涨"不能只问 footprint；③ 迟滞的收敛会被**扫描时机**骗——降级每轮只走一级，而重算只在相机写入与解码完成时触发，**空闲不会自己收敛**（第一版只 drain 一次就宣布"停住"，报 1/2，真实稳定点 1/4）。
  - **又一条"落点压在边界上"的教训**：迟滞的"该降就降"反向断言第一版缩到 0.4 倍，需求正好是 480（1/8 档宽），浮点误差让"刚好覆盖需求"在相邻两档之间跳，缺陷因此藏住了；改成 1/3 倍（需求 400，离两侧都有两成余量）才有效。只有"不降档"那一条断言的话，"永远不降档"的实现在断言上完全通过——那是比抖动更糟的缺陷。
  - 已知边界：扫描仍是 **O(场景元素)**（1000 元素下 P99 0.411 ms，离 8ms 预算还有二十倍，所以**没有**改成空间索引）；预加载边距 256 点**没有实测依据**（是拍的）；真实系统内存压力造不出来（报告第 7 节那份压力序列是**构造的**）；遮挡感知的适配仍未做。
  - 验证：Debug / Release 均无错误无警告；`--selftest` **380/380** 通过（Debug 与 Release 各一遍）；`--perf-report` 退出码 0；八张快照与四个应用包（三个 release + 一个 debug 实测包）已重建；`BASELINE-A.sha256` 按 **45 个文件**重新生成（比上一轮多四份：`MemoryPressureMonitor.swift`、`PerformanceProbe.swift`、`PerformanceReport.swift`、`PERF_REPORT_B2.md`），`--check` 通过。基线提交仍为 `bfe2fd2`，改动**未提交**。
  - 快照的可比性：**空画布那 6 张与上一轮逐字节相同**；两张内容图**不可逐字节比对**——合成素材的 `AssetID()` 每个进程一份新 UUID，噪声种子由它派生，画面内容本来就每次不同（实测同一二进制连跑两次，像素流就不一样）。要让内容快照也能做逐字节回归，得先把演示素材的 ID 确定化，属下一轮。
  - 未验证（按 §9 口径不使用"已完成"）：帧率 / GPU 占用 / 真机手感（Instruments 未跑）；真窗口 Liquid Glass 观感；多显示器与屏幕倍率的实机行为（本机只有一块屏）；真实系统内存压力；鼠标滚轮系数（无鼠标）；面板拖宽手感与 `⌘⇧B` 换画布。
  - 送审清单见 `pin-macos/CODEX_REVIEW_REQUEST.md`（逐文件改动、五次注入的逐字红项、六个判断、未验证清单），交接说明见 `pin-macos/HANDOFF.md` 的「批次 B2」一节。
- Codex 独立复审批次 B2：主体实现与既有回归通过，但当前**暂不验收**，等待三项代码返修与性能证据补齐。
  - 发现图片请求在成功前就写入 `displayed`；瞬时 `.failed` 后，同素材同档位被永久视为“已显示”，后续刷新不会重试。隔离副本注入“首次失败、第二次成功”的提供者后，仍只请求 1 次且图层保持空白。
  - 当前 64–512 MB 只约束 `ImageCache.entries`，没有计入 `CALayer.contents` 持有的旧图/当前图、在途解码与临时缓冲，不符合路线图 §2.3 的总预算口径。隔离副本中 critical 压力已将缓存清零，但画布图层仍持有 3840×2160 像素。
  - `setMotionConfiguration` 只替换参数，不刷新可见性或 LOD；运行时把预加载边距从 0 调到 400 点后，本应进入预加载区的元素仍未建层，与路线图 §3.3 的实时调参接口目标不一致。
  - `PERF_REPORT_B2.md` 的测量台不开窗口，未覆盖真实首帧、保存场景恢复、实际帧时间/FPS、GPU 与玻璃浮层开关对照；报告已诚实注明边界，但尚未满足路线图 §2.4、§5 的完整 B 阶段出口证据，不能把 87 ms 下界或主线程工作耗时直接判为完整“达标”。
  - 验证：45 文件清单通过；Debug / Release 构建与原有 **380/380** 自检通过；`--perf-report` 可重复运行；8 张隔离快照无新增重叠或溢出。隔离副本新增定向回归后为 **387 项中 4 项失败**，失败项正好对应上述三项代码缺陷（失败重试 2 条、运行时参数 1 条、图层内存 1 条）。
  - 本次按日常细颗粒迭代管理，不修改 CC 实现；代码与复审日志同步到 GitHub 临时分支 `native/pin-macOS/canvas-kernel-a`，不合并长期分支、不打标签、不作为正式发布。
### 2026-09-16

- 完成 Pin 原生批次 A 独立审核：Debug / Release 构建通过，81/81 项自检通过，六档深浅快照与 Pin.app 启动已验证；发现场景增量未接入渲染器、输入接缝不足和交付快照未冻结三项 P1，批次 A 暂不通过，待返修复审。本次仅记录审核，不修复实现、不发版。
- 将原生客户端长期分支由 `pin` 重命名为 `pin-macOS`，原生版本标签仍使用 `pin-native-*`。
- 调整 GitHub 维护规范：开发或审查后自动更新本地版本日志；提交、推送、合并、打标签和发布必须等待用户确认版本号与目标分支。

### 2026-09-15

- 新增 `GITHUB_MAINTENANCE_WORKFLOW.md`，约束 Codex 完成开发或审查 CC 后的测试、版本记录、数据隔离、提交和 GitHub 同步流程。
- 新增根目录 `AGENTS.md`，要求后续 AI 在任务收尾前读取并执行上述规范。

## v1.0.0-draft.2 - 2026-09-11

- SwiftUI 草案升级为 macOS 26 原生 Liquid Glass 视觉实验，不连接后端、不读取或修改现有截图素材。
- 使用系统玻璃按钮重做侧栏底部操作，并让窗口顶部工具栏自动采用 Liquid Glass 外观。
- 截图库新增悬浮玻璃操作坞，可调整缩略图、筛选排序，并根据图片选择状态切换批量操作。
- 新增大图预览草案及玻璃工具栏，可体验平滑缩放、旋转、收藏、取色、分享和图片信息入口。
- 所有功能图标统一使用 SF Symbols，并将截图内容层保持为非玻璃背景，突出素材本身。

## v1.0.0-draft.1 - 2026-09-10

- 新建独立的 SwiftUI macOS 原生视觉草案，不连接后端、不读取或修改现有截图素材。
- 以原生侧栏、分栏导航、窗口工具栏和系统控件还原截图管理与分析工作区框架。
- 使用虚构静态内容搭建可调密度截图网格、选择态、搜索、右键菜单和批量操作样式。
- 新增待归类分析、分析文件夹和分析列表视觉框架，可在截图与分析工作区间切换。
- 保存现有 HTML 版本与 SwiftUI 草案的对照截图，并生成可双击运行的 `DesignPeek Draft.app`。

## v0.3.3 - 2026-09-10

- 新增局域网手机传图：电脑端一键生成二维码，手机与电脑连接同一 Wi-Fi 或手机热点后即可扫码访问。
- 手机上传页支持从相册多选图片、显示总大小、逐张传输进度、失败提示及连续追加上传。
- 二维码使用 30 分钟临时连接，关闭弹窗即撤销；图片只在当前局域网传输，不经过云端。
- 上传图片统一进入“新添加截图”，继续复用 HEIC 转 PNG、自动 OCR 和电脑端实时刷新流程。
- 优先识别 Mac 的真实 Wi-Fi 网卡地址，减少 VPN 开启时二维码地址不可访问的问题。
- 单张图片限制为 30 MB，电脑弹窗实时显示已接收数量，并支持刷新二维码和复制连接地址。
- 修复删除新上传图片时清理分析引用可能失败的问题。

## v0.3.2 - 2026-09-09

- 分析侧栏重构为“待归类分析”和“文件夹”两个清晰区域，新建分析默认进入待归类区。
- 分析与文件夹改用 Remix Icon 图标和明确的缩进、数量及状态标记，文件夹不再形成嵌套层级。
- 支持将分析拖入文件夹、拖回待归类区，并支持在同一分组内拖拽调整顺序。
- 文件夹仅用于归类分析，统一“新建、重命名、删除文件夹”的界面文案；删除文件夹会保留分析与截图。
- 旧版分析自动迁移为独立分析记录，完整保留原截图证据和报告，原项目容器随后只作为文件夹使用。
- 分析归类和排序会保存在本地数据中，不改变截图所属素材文件夹及 Finder 中的真实位置。

## v0.3.1 - 2026-09-09

- 大图查看器新增独立平滑缩放，不再需要缩放整个网页；支持滚轮或触控板缩放、放大后拖拽平移及双击切换 100%。
- 新增缩小、放大、适合窗口、原始尺寸操作，并显示当前实际缩放比例。
- 新增临时左右旋转、屏幕取色并复制 HEX 色值，旋转仅影响查看，不修改原始图片。
- 大图底部新增设计师工具栏，统一收纳收藏、备注、复制图片、下载原图、访达定位和图片信息。
- 图片信息支持查看像素尺寸、文件格式、文件大小、素材文件夹和添加时间。
- 补充 `+`、`-`、`0`、`1`、`[`、`]` 等查看快捷键，保留方向键切换和 Esc 关闭。
- 工具栏采用本地 Remix Icon v4.9.0 图标资源，离线可用并保留官方许可证。

## v0.3.0 - 2026-09-08

- 分析工作流改为“选中截图 → 开始分析 → 输入一个问题”，不再要求先创建分析项目。
- 新增独立“分析对话”数据层；截图只作为附件引用，删除、重命名或移动对话均不影响素材文件及 Finder 位置。
- 原分析项目改为归档项目，支持新建、重命名、删除、层级拖放，并可将分析对话拖入项目整理；旧项目以只读兼容方式保留。
- 移除分析前的类型、方法、维度和边界确认；AI 根据问题自动选择最多 3 个相关观察角度。
- 新增五个快捷问题角度；普通分析不要求截图排序，仅“还原关键操作路径”显示顺序确认。
- 默认结果升级为“决策速览”：一行结论、最多 3 条核心发现、1-3 条优先行动及证据缺口，详细证据默认折叠。
- 单 App 统一显示“竞品诊断”，多 App 才显示“对比”；发现明确区分“竞品观察、对我的启示、建议行动、待验证”。
- 图片证据改用可读场景名，不再向用户展示截图 ID；分析对话支持复制本地链接、重命名和删除。
- 取消预先生成维度的模型调用；逐图客观观察最多 3 张并发，并按图片版本与模型缓存，后续换问题时可复用。
- 已用 Kimi 对真实截图完成端到端验证，新结果严格输出 1 条结论、3 条发现、3 条行动和证据缺口。

## v0.2.3 - 2026-09-08

- 截图页左侧新增“已收藏”分类和实时数量，可跨素材文件夹筛选收藏图片。
- 原图查看器中的“顶呱呱”改为“收藏 / 取消收藏”双状态交互。
- 收藏仅写入图片元数据，不改变素材文件夹归属，不移动 Finder 中的真实文件。
- 在“已收藏”视图中取消收藏后，背景列表和收藏数量会立即更新。

## v0.2.2 - 2026-09-08

- 修复 Kimi 长截图分析时默认思考过程占满输出额度、导致最终结构化内容无法解析的问题。
- Kimi 分析请求改为非思考模式，优先保证图片证据 JSON 稳定输出。
- 新增逐张截图失败日志与项目失败明细，不再只显示笼统的“所有截图失败”。

## v0.2.1 - 2026-09-08

- AI 模型选择新增 Qwen 千问、DeepSeek 和 Kimi。
- 三家服务统一接入现有的 Key 管理、图片连接测试和分析流程。
- 切换服务商时自动填入官方视觉模型与接口地址，并显示配置提示。
- DeepSeek 标注当前视觉模型为实验版，避免误用不支持图片的文本模型。

## v0.2.0 - 2026-09-08

- 重构分析项目使用动线：从批量选图加入项目后，直接进入对应分析项目。
- 修复待分析项目页面不显示已加入截图的问题，旧项目素材保持不变。
- 新增必填分析问题和选填使用场景，支持模块对比、视觉风格和操作动线问题模板。
- 引入 Nielsen 可用性原则、Cognitive Walkthrough、Gestalt、WCAG、Baymard 电商 UX 及 HEART 的问题驱动分析框架。
- AI 先根据问题生成 4-8 个分析维度，用户可编辑、新增或删除，确认后才正式读取截图。
- 确认维度时可通过拖动或箭头调整截图顺序，动线分析按用户确认的步骤顺序处理。
- 分析改为逐张提取证据后再综合，支持证据缓存、处理进度、单图失败容错和最近 5 次分析记录。
- 结果升级为以图片证据为主的维度对比看板，区分观察、解读、风险、机会点和建议，并附置信度与可点击的原图证据。
- 分析菜单新增 AI 设置，支持 Gemini、Claude 和 OpenAI 兼容接口，可配置模型、网关地址并测试连接。
- API Key 优先保存在 macOS 钥匙串，不写入项目数据、浏览器存储或 Git，并继续兼容旧 `.env` 配置。

## v0.1.7 - 2026-09-07

- 修复旧截图没有文字索引、导致搜索不到图片内容的问题。
- 接入 Mac 本机 Vision 文字识别，无需用户额外安装 OCR 模块，图片不会上传到第三方。
- 服务启动后自动为旧截图补齐文字索引，新截图上传后自动建立索引。
- 搜索升级为全局搜索，不再受当前素材文件夹或“新添加截图”筛选限制。
- 搜索范围覆盖图片文字、素材文件夹名称、来源 App、备注和文件名。
- 首次建立索引时显示识别进度，并自动刷新搜索结果。

## v0.1.6 - 2026-09-07

- 顶部截图入口调整为“新添加截图”，移除“全部截图”和“顶呱呱”入口。
- 新上传及自动采集的截图统一保存到电脑中的“新添加截图”真实目录。
- 素材文件夹升级为真实磁盘目录，拖拽或批量加入时同步移动图片文件。
- 素材文件夹重命名会同步修改磁盘目录；删除文件夹时，图片会移回“新添加截图”。
- 竞品 App 改为纯前台来源筛选，不再决定图片的后台存储位置。
- 迁移已有素材：按现有素材文件夹归属移动，未归类素材保留在“新添加截图”。

## v0.1.5 - 2026-09-07

- 竞品 App 分类默认折叠，点击标题可按需展开或收起。
- 修复批量框选时出现浏览器原生浅蓝色选中态的问题。
- 截图右键菜单新增“打开本地图片文件夹”，单选时在访达定位图片，多选时打开对应素材目录。
- 批量管理新增“加入素材文件夹”，可在弹窗中选择已有素材文件夹。
- 右键菜单支持批量选中状态，可对所选截图打开本地目录或批量删除。

## v0.1.4 - 2026-07-08

- 批量管理栏移除“移入文件夹”按钮，素材归档统一通过拖拽到左侧素材文件夹完成。
- 批量管理栏保留单一“加入分析项目”入口，弹窗内可选择已有分析项目或新建分析项目。
- 右侧截图网格上下间距加大到两倍，提升鼠标框选时的可控性。

## v0.1.3 - 2026-07-08

- 将“项目文件夹”统一改名为“素材文件夹”，和“分析项目”区分开。
- 批量管理栏中素材整理动作改为“移入文件夹”，分析动作改为“加入分析项目”。
- 移除批量管理栏里的“创建新项目/新建文件夹”入口，新建素材文件夹只保留在左侧素材文件夹区。

## v0.1.2 - 2026-07-08

- 新增左侧项目文件夹管理：支持新建、重命名、删除自定义文件夹，删除文件夹不删除截图素材。
- 支持将右侧截图拖入左侧项目文件夹，批量选中后拖动任意已选截图可一起调入文件夹。
- 右键截图菜单新增“复制图片”功能，浏览器不支持图片剪贴板时自动复制图片地址。
- 批量管理模式新增鼠标框选，可大范围选择截图，并支持批量调入已有或新建文件夹。
- 新增后端文件夹索引 `data/folders.json`，只保存截图引用，不移动原始图片文件。

## v0.1.1 - 2026-07-08

- 新增截图卡片右键删除功能。
- 新增实时同步，手机端上传/截图后，电脑网页自动显示新截图。
- 收录 HEIC/HEIF 图片格式修复：iPhone 图片即使文件名伪装成 `.png`，后端也会识别并转成浏览器可显示的 PNG。
- 删除截图时同步清理项目引用，避免项目里残留失效卡片。

## v0.1.0 - 2026-07-08

- 保存 DesignPeek 初始 Git 版本。
- 添加 Git 忽略规则，排除本地密钥、虚拟环境、运行日志、生成数据和用户截图。
