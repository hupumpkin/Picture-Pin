# Picture-Pin GitHub 维护与发布确认规范

> 本文是给 Codex、CC 以及其他 AI 协作者的强制执行规范。
> 目标：每次完成开发或审查后自动更新本地版本记录；只有用户明确确认发版时，才将指定版本同步到 GitHub。

## 1. 产品线与分支

本仓库使用一仓库、多产品线的方式管理：

| 分支 | 产品线 | 规则 |
| --- | --- | --- |
| `main` | 已验收稳定版本 | 只接受已审查、已验证的合并 |
| `designpeek` | 原 DesignPeek 网页版 | 默认冻结，只做必要修复 |
| `pin-codex` | Codex 直接开发的 Pin Web | Codex 的 Web 主开发线 |
| `pin-cc` | Codex 指挥、CC 执行的 Pin Web | CC 结果的独立验证线 |
| `pin` | 后续原生客户端 | 与 Web 分支独立演进，不直接合并 Web 实现 |

长期分支不直接写功能。每次任务必须从对应长期分支创建临时分支：

```text
codex/designpeek/<topic>
codex/pin-web/<topic>
cc/pin-web/<topic>
native/pin/<topic>
fix/<product>/<topic>
```

`pin-codex` 和 `pin-cc` 默认保持独立。未经审查，不得把一条线的最新代码直接覆盖另一条线。

## 2. 何时触发什么流程

出现以下任一情况，必须自动执行本地检查和版本记录更新：

1. Codex 完成一项开发或修复。
2. Codex 完成对 CC 提交的代码审查。
3. 用户要求记录一次迭代。

只有出现以下明确指令，才允许执行 GitHub 发布流程：

- “确认更新并发版”
- “把这个版本上传 GitHub”
- “将某版本推送到某分支”

用户只说“完成了”“看看效果”“继续优化”时，不得推送 GitHub、创建正式标签或更新稳定分支。

如果代码仍未完成、测试失败、存在未解释的阻塞问题，不能把它标记为已交付，也不能创建正式版本标签；可以保留本地临时提交，但必须在结果中说明原因。

## 3. 开始工作前

在修改代码前，必须确认：

- 当前目录、当前分支和工作区状态。
- 本次任务对应的产品线和目标分支。
- 是否存在用户尚未提交的修改；有则保留，不能覆盖或回退。
- 是否使用了独立的运行数据目录。
- 是否读取了本次任务对应的路线图、交接文档和版本记录。

交给 CC 的任务还必须固定基础提交号，并在任务说明中写清：目标、范围、明确不做的内容、允许修改的文件、禁止修改的文件、验收标准和运行方式。

## 4. 完成开发或审查后的检查

按以下顺序执行，并记录结果：

1. 检查 Git 差异，确认没有无关文件、临时文件、测试素材或密钥。
2. 执行语法检查、单元检查或项目已有测试。
3. 启动对应服务，验证用户主流程。
4. 对页面功能做实际浏览器验证；涉及布局时至少检查桌面、窄窗口和移动尺寸。
5. 检查素材数量、素材真实位置、画布数据和分析数据没有意外变化。
6. 检查 `.env`、API Key、Cookie、用户截图和运行数据没有进入 Git 差异。
7. 自动更新对应版本记录；只记录已实际完成并验证的内容，并标明“本地迭代”或“已发布”。

审查 CC 时，必须额外确认：

- CC 修改是否只发生在约定范围内。
- CC 的提交是否建立在约定基础提交上。
- CC 是否误改用户素材、运行数据或原有功能。
- 版本日志中的“已验证”是否有实际证据支持。
- 失败项、人工确认项和已知限制是否写明。

## 5. 本地提交规则

提交前必须再次执行：

```text
git status
git diff --check
git diff --stat
```

提交信息使用清晰的产品线前缀：

```text
DP: 修复搜索结果匹配
PIN-CODEX: 增加画布图片来源标记
PIN-CC: 接入花瓣内嵌浏览
PIN-NATIVE: 更新原生侧栏草案
DOCS: 更新仓库维护规范
```

一个本地提交尽量对应一个完整改动。没有通过验证的代码，不得使用“release”“stable”“完成”等容易误导的提交说明。

小迭代可以只更新本地代码和版本记录，不必单独推送 GitHub。是否形成正式发布版本，由用户决定。

## 6. 用户确认后的 GitHub 发布流程

收到明确发版指令后，必须先向用户确认以下两项，不能自行猜测：

1. 版本号，例如 `pin-cc-v0.3.0`。
2. 目标分支，例如 `pin-cc` 或 `main`。

用户确认版本号和目标分支后，按以下顺序操作：

1. 检查当前提交、目标分支和工作区状态。
2. 确认差异中没有素材、运行数据、密钥或无关文件。
3. 将代码提交到当前任务的临时分支。
4. 推送临时分支到 GitHub。
5. 创建 Pull Request，目标为用户确认的长期分支，并写入改动摘要、测试结果、已知限制、数据安全确认和版本号。
6. 通过审查后合并到用户确认的长期分支。
7. 用户确认需要正式发布时，再创建对应版本标签和 GitHub Release。
8. 同步完成后，重新读取远端分支和提交号，确认推送目标正确。

禁止把未确认的 CC 实验分支自动合并到 `main`，也禁止因为版本日志已更新就自动推送 GitHub。

如果没有远端地址、没有登录权限或推送失败：

- 不得声称“已同步 GitHub”。
- 保留本地提交和版本日志，不重复改写提交历史。
- 明确告诉用户停在“本地已提交、远端未同步”哪一步。
- 下次继续时先检查远端是否已经收到提交，避免重复推送或重复创建版本。

## 7. 数据与密钥红线

GitHub 只管理代码、文档和必要的静态资源，不管理用户运行数据：

- 不提交 `screenshots/`、`data/`、`.env`、真实 API Key、Cookie 或日志中的敏感内容。
- 不用用户真实素材作为自动化测试数据。
- CC 和 Codex 使用不同的运行数据目录，测试前先确认目录指向。
- 涉及文件移动、删除、迁移时，先备份并记录数量；测试结束后复核数量和路径。
- 发现密钥已经进入差异时，立即停止推送，先清理提交并提醒用户更换密钥。

## 8. 版本标签与版本日志

四条产品线使用独立标签，不共用一套数字：

```text
designpeek-vX.Y.Z
pin-codex-vX.Y.Z
pin-cc-vX.Y.Z
pin-native-vX.Y.Z
```

版本日志至少包含：版本号、日期、精简改动、验证结果、已知限制。文档规范或协作流程变更不强行增加产品版本号，应单独记录为仓库维护记录。

禁止：

- 为未完成工作提前打正式标签。
- 用同一个版本号代表 Pin Web、CC Web 和原生客户端。
- 只更新版本号而不写实际改动。
- 把“代码已提交”写成“用户流程已验证”。

## 9. 任务结束时必须向用户报告

结束回复必须包含：

1. 完成了什么。
2. 验证了什么。
3. 当前分支和提交号。
4. GitHub 是否已同步；若已同步，给出分支或 Pull Request 链接。
5. 是否创建版本标签。
6. 如果尚未发版，明确写“本地已记录，等待用户确认后上传 GitHub”。
7. 未完成、需人工确认或存在风险的事项。

没有真实证据时，使用“未验证”“待人工确认”“本地已提交但未推送”，不能使用“已完成”“已发布”等表述。

## 10. 最终自检清单

每次开发或审查结束都必须完成：

```text
[ ] 当前分支正确
[ ] 用户已有修改未被覆盖
[ ] 代码检查通过
[ ] 浏览器主流程通过
[ ] 窄窗口/移动尺寸已检查（涉及 UI 时）
[ ] 用户素材和运行数据数量、位置正常
[ ] 没有密钥、Cookie、截图和临时文件进入差异
[ ] 版本日志已更新
[ ] 提交信息清晰
[ ] 明确写出“本地已记录，等待用户确认后上传 GitHub”或真实同步状态
[ ] 最终回复包含分支、提交、同步状态和风险
```

只有用户明确要求发版时，才额外完成：

```text
[ ] 用户已明确确认需要发版
[ ] 用户已确认版本号
[ ] 用户已确认目标分支
[ ] 临时分支已推送
[ ] Pull Request 目标分支正确
[ ] 版本标签只在用户确认后创建
```
