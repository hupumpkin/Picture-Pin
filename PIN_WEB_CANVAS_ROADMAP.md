# Pin Web 无限画布开发路线图

## 版本定位

- 建议版本号：`v0.4.0`
- 版本主题：素材工作区与无限画布基础版
- 开发方式：继续使用当前工作对话和 Git 分支，不创建额外分支
- 技术方向：保留现有 HTML、CSS、JavaScript 与 FastAPI，引入本地 Konva.js 画布引擎

## 产品范围

本次迭代把现有截图管理页面改造成“素材来源 + 素材侧边栏 + 无限画布”的工作区。本期不进行品牌升级，不接入真实外部素材网站，也不开发分析功能。

## 一、版本与素材保护

1. 不迁移、不删除、不改写现有截图文件。
2. 保留分析模块的数据和后端代码，只隐藏当前界面入口。
3. 画布中的图片只引用截图素材，不复制或移动 Finder 中的原图。
4. 完成后更新 `VERSION_HISTORY.md` 并创建 Git 提交。

## 二、整体网页框架

页面调整为三层结构：

1. 素材来源栏：截图、花瓣、站酷。
2. 素材侧边栏：截图分类、素材文件夹、搜索和图片缩略图。
3. 主工作区：无限画布。

本期只有“截图”来源可用；花瓣和站酷显示为占位入口，不连接真实网站。

## 三、截图素材接入

1. 复用现有截图、收藏、素材文件夹和搜索接口。
2. 支持查看“新添加截图”“已收藏”和各素材文件夹。
3. 素材侧边栏显示可拖动的图片缩略图。
4. 从侧边栏拖入画布时只创建画布元素，不改变素材归属。
5. 保留手机传图和本地上传入口。

## 四、无限画布

使用 Konva.js 实现：

1. 鼠标或触控板平移画布。
2. 滚轮或触控板缩放画布。
3. 从素材侧边栏拖入图片。
4. 自由移动画布中的图片。
5. 通过控制点等比例缩放图片。
6. 选中图片后删除。
7. 图片保持原始宽高比例。
8. 删除画布元素不删除素材原图。
9. 重新打开页面后恢复画布内容与视口位置。

## 五、多画布数据框架

本期界面只开放“默认画布”，底层数据结构支持：

1. 多个画布 ID、名称和创建时间。
2. 每个画布独立的图片元素。
3. 每个画布独立的缩放比例和视口位置。
4. 后续新增、重命名、切换和删除画布。
5. 数据结构包含版本号，为未来迁移保留升级入口。

图片元素保存素材 ID、位置、尺寸、旋转角度和层级，不保存图片文件本身。

## 六、基础工具栏

画布底部建立工具栏框架，预留选择、手型移动、缩小、放大、适合画布、缩放比例、撤销和重做。本期只完成布局与禁用状态，不实现按钮功能；画布直接拖动和滚轮缩放仍然可用。

## 七、自动保存

1. 添加、移动、缩放或删除元素后延迟自动保存。
2. 视口位置与缩放比例独立保存。
3. 页面关闭前补充保存。
4. 后端保存结构化 JSON，并采用原子写入。
5. 数据缺失或损坏时回退到空白默认画布，不影响截图素材。

## 八、验收标准

1. 原有截图和素材文件夹正常显示。
2. 图片可以从侧边栏拖入画布。
3. 图片可以移动、等比例缩放和删除。
4. 画布可以平移和缩放。
5. 刷新或重启服务后恢复画布内容与视口。
6. 花瓣、站酷入口仅作占位且不会产生错误。
7. 分析入口隐藏，原分析数据完整保留。
8. 原有图片数量和 Finder 文件位置保持不变。
9. 常用桌面窗口尺寸下没有遮挡和布局错位。
10. 版本记录与 Git 提交完整。

## 九、花瓣内嵌浏览实验（P0.3.0）

在素材来源栏把「花瓣」从占位入口变成可用来源：面板用 iframe 直接加载 `https://huaban.com/`，用户在面板内自行登录、搜索、浏览，右键复制图片后在画布按 `Command + V` 粘贴。

实现要点：

1. 面板切换：花瓣与截图共用左侧同一个悬浮面板位置，切换来源即切换面板，两个面板各自记忆折叠状态。
2. 延迟加载：iframe 在用户第一次点开花瓣时才创建并加载，不在每次打开 Pin 时请求外部站点。
3. 不代理：花瓣页面直连，登录 Cookie 只留在浏览器与花瓣之间，不经过 Pin 后端，也不写入任何数据文件。
4. 粘贴通道：画布粘贴的图片复用现有 `/api/upload` 接口，进入「新添加截图」后按素材 ID 落到当前视口中央，与拖入的素材走同一条数据链路。
5. 权限克制：iframe 只放开 `clipboard-write`，不放开 `clipboard-read`。

已验证：

- 花瓣页面当前没有 `X-Frame-Options` 或 CSP `frame-ancestors` 限制，iframe 嵌入不被策略阻止。
- 面板切换、iframe 加载、粘贴入画布、刷新后画布恢复均已通过浏览器自动化验证。
- 常用桌面窗口尺寸下花瓣面板与分析面板不重叠、无横向溢出。

未解决 / 待人工确认：

- iframe 内登录状态能否长期保持，取决于浏览器对第三方 Cookie 的策略（Safari 默认拦截，Chrome 逐步收紧），需要真实浏览器 + 真实账号确认。
- 花瓣的反爬风控会对无头浏览器返回 405 异常访问页（顶层直接打开同样如此），因此登录与浏览体验只能由用户在真实浏览器中验证。
